spring:
  datasource:
    url: jdbc:postgresql://localhost/book_catalog
    username: codecorn
    password: codecorn
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 100
        order_inserts: true
